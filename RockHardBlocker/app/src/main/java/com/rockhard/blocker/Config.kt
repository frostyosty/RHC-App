package com.rockhard.blocker

object Config {
    // Toggled via CLI. If false, the Guardian allows access to Android Settings so you can uninstall.
    const val UNINSTALL_PROTECTION_ENABLED = true
}
